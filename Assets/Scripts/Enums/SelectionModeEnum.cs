public enum SelectionMode 
{
    Default,    // Normal selection, clears previous selection
    Additive,   // Adds to current selection (Shift key)
    Subtractive // Removes from current selection (Ctrl key)
}